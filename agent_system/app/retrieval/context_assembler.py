from __future__ import annotations

from app.retrieval.context_builder import ContextBuilder


class ContextAssembler(ContextBuilder):
    """Backward-compatible alias for the new context builder."""

